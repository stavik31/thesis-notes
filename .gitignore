# OS
.DS_Store
Thumbs.db

# Obsidian
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# Editor
*.swp
*.swo

# Python env / build caches (do not commit)
.venv/
unsloth_compiled_cache/

# Code data and outputs (large files — keep local only)
code/data/
code/outputs/
code/map.html

# Editor
.vscode/

# Python bytecode
__pycache__/
*.pyc

# Test-folder generated artifacts (regenerable from the scripts; some are >1GB).
# Keep the .py reference implementations, never the data they produce.
code/tests/**/*.csv
code/tests/**/*.json
code/tests/**/*.log
code/tests/**/*.png
code/tests/**/*.html
